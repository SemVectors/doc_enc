defaults:
  - base_sent_encoder_config

emb_conf:
  emb_kind: TOKEN_WITH_POSITIONAL
  emb_dim: 320

encoder_kind: FNET
hidden_size: 512
num_layers: 3
dropout: 0.1
num_heads: 8
filter_size: 2048
pooling_strategy: FIRST
