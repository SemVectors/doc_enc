# @package _global_
defaults:
  - /base_config
  - _self_

trainer:
  max_updates: 900
  eval_every: 300
  switch_tasks_every: 10

tokenizer:
  vocab_path: /home/dvzubarev/data/paral_data/undata0/sentencepiece.bpe.model

batches:
  sents_batch_iterator_conf:
    batch_generator_conf:
      input_dir: /home/dvzubarev/data/paral_data/undata0/mini

  docs_batch_iterator_conf:
    include_datasets:
      - mini
    batch_generator_conf:
      input_dir: /home/dvzubarev/Workspace/projects/semvectors/datasets/temp
      batch_sent_size: 128
